// @ts-check

/**
 * @type {import('./types').PrettierConfig}
 */
const config = {
    arrowParens: 'avoid',
    bracketSpacing: true,
    endOfLine: 'lf',
    printWidth: 120,
    semi: true,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'none',
    useTabs: false
};

module.exports = config;
